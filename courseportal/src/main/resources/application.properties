spring.application.name=courseportal

# Database (use env vars in production, fallback for local)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/course_portal}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:Saitej@406}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ? REMOVE this (Hibernate auto-detects now)
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Server (Render will inject PORT)
server.port=${PORT:8080}

# CORS (set FRONTEND_URL in Render dashboard)
app.frontend.url=${FRONTEND_URL:http://localhost:3000}